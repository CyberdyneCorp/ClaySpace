name: ClaySpace
options:
  bundleIdPrefix: com.cyberdyne
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true

# ClayCore (https://github.com/CyberdyneCorp/ClayCore) is consumed as a
# sibling checkout pinned to a release tag; its Package.swift wraps the
# prebuilt dist/claycore.xcframework (run tools/build_xcframework.sh in
# the ClayCore repo after checking out the tag). Pinned: v0.5.0.
packages:
  claycore:
    path: ../../ClayCore

targets:
  ClaySpace:
    type: application
    platform: iOS
    deploymentTarget: "18.0"
    sources:
      - ClaySpace
    dependencies:
      - package: claycore
        product: claycore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.cyberdyne.clayspace
        TARGETED_DEVICE_FAMILY: "2"          # iPad only
        SWIFT_VERSION: "6.0"
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown"
        INFOPLIST_KEY_UIStatusBarHidden: YES
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: 2C69VJZSNR
        # claycore is a C++-implemented static library behind a C ABI:
        # the app links libc++; the xcframework's simulator slice is
        # arm64-only, so exclude x86_64 there.
        OTHER_LDFLAGS: "-lc++"
        "EXCLUDED_ARCHS[sdk=iphonesimulator*]": x86_64
